/** Policy contract carried on MCP requests and embedded in structured responses. */
export { MCP_POLICY_VERSION } from "@memorynodeai/shared";

/** MCP tool manifest version — bump per docs/PLAN.md §4 when tools change meaningfully. */
export const TOOL_MANIFEST_VERSION = "1.0.2";
